[Unit]
Description=TaskQueue PostgreSQL backup
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
User=deploy
Group=deploy
Environment=TASKQUEUE_APP_DIR=/opt/taskqueue
Environment=TASKQUEUE_BACKUP_DIR=/var/backups/taskqueue
ExecStart=/opt/taskqueue/deploy/scripts/backup-postgres.sh

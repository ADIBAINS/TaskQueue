{
  "name": "@taskqueue/scheduler",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-vm-modules --import tsx node_modules/.bin/vitest run",
    "lint": "prettier --check src/"
  },
  "dependencies": {
    "@taskqueue/shared": "workspace:*",
    "cron-parser": "^5.5.0",
    "kafkajs": "^2.2.0",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}

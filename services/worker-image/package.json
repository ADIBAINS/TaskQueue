{
  "name": "@taskqueue/worker-image",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "prettier --check src/"
  },
  "dependencies": {
    "@taskqueue/shared": "workspace:*",
    "sharp": "^0.33.0",
    "kafkajs": "^2.2.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  }
}

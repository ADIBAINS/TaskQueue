{
  "name": "@taskqueue/state-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "migrate": "tsx src/migrate.ts",
    "test": "vitest run --passWithNoTests",
    "lint": "prettier --check src/"
  },
  "dependencies": {
    "@taskqueue/shared": "workspace:*",
    "pg": "^8.13.0",
    "ioredis": "^5.4.0",
    "kafkajs": "^2.2.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  }
}
